Algoritmo ej4
		//Suponiendo que hemos introducido una cadena por teclado que representa una frase (palabras
		//separadas por espacios), realiza un programa que cuente cuántas palabras tiene.
	
	espacio <- 1
	Escribir "Introduce una frase: "
	Leer frase
	Para conta<-1 Hasta longitud(frase) Con Paso 1 Hacer
		letra <- subcadena(frase,conta,conta)
		Si letra = " " Entonces
			espacio <- espacio + 1
		FinSi
	FinPara
	Escribir "Hay " espacio " palabras."
FinAlgoritmo
